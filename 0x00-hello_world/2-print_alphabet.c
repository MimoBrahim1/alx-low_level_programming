#include <stdio.h>
/**
 * main-prints the alphabet in lowercase, followed by a new line
 * Return: 0
 */
int main(void)
{
	char alph = 'a';

int main () {
   char ch;

   for(ch = 'a' ; ch <= 'z' ; ch++) {
      putchar(ch);
   }

    putchar ('\n');

   return(0);
	while (alph <= 'z')
	{
	putchar (alph);
	alph++;
	}
	putchar ('\n');
	return (0);
}
